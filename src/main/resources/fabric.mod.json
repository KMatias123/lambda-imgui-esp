{
  "schemaVersion": 1,
  "id": "${modId}",
  "version": "${modVersion}",
  "name": "${modName}",
  "description": "${modDescription}",
  "authors": ["${modAuthors}"],
  "contact": {
    "homepage": "https://github.com/KMatias123/lambda-imgui-esp",
    "sources": "https://github.com/KMatias123/lambda-imgui-esp"
  },
  "license": "GNU General Public License v3.0",
  "environment": "client",
  "entrypoints": {
    "client": [
      {
        "adapter": "kotlin",
        "value": "dev.kmatias.lambdaimguiesp.LambdaImGuiESPClient"
      }
    ]
  },
  "mixins": [
    "${modId}.mixins.common.json"
  ],
  "accessWidener": "${modId}.accesswidener",
  "depends": {
    "lambda": ">=${lambdaVersion}",
    "fabricloader": ">=${fabricLoaderVersion}",
    "minecraft": "~${minecraftVersion}",
    "fabric-api": ">=${fabricApiVersion}+${minecraftVersion}",
    "fabric-language-kotlin": ">=${kotlinFabricVersion}",
    "java": ">=${javaVersion}"
  }
}
